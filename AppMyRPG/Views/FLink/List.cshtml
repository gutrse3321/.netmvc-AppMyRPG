@model IEnumerable<AppMyRPG.Models.FriendLink>
@{ Layout = null; }
@foreach (var item in Model)
{
    <span class="friendlink-item">
        @item.FLName：<a href="http://@item.FLUrl" target="_blank" title="@item.FLUrl">@item.FLUrl</a>
    </span>
}
